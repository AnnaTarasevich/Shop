&НаКлиенте
Процедура ПриОткрытии(Отказ)
    Если Параметры.Свойство("Номенклатура") Тогда
        Номенклатура = Параметры.Номенклатура;
        Элементы.Список.Отбор.Номенклатура.Значение = Номенклатура;
    КонецЕсли;
КонецПроцедуры