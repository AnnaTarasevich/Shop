&НаКлиенте
Процедура ПриОткрытии(Отказ)
    Если Параметры.Свойство("НоменклатураСПР") Тогда
        Номенклатура = Параметры.НоменклатураСПР;
        Элементы.Список.Отбор.НоменклатураСПР.Значение = Номенклатура;
    КонецЕсли;
КонецПроцедуры

